# thwu1 MLX Proxy Candidate

This is a Mac-native proxy candidate derived from the mirrored public `thwu1/parameter-golf` repository.

Purpose:

- run locally on Apple Silicon through MLX
- validate architecture/training/export changes cheaply on the official challenge data
- prepare ideas for later promotion to real H100 record-track runs

Notes:

- this is a proxy lane, not a leaderboard-valid record run
- the script source was copied from `research-experiments/cache/external/thwu1-parameter-golf/train_gpt_mlx.py`
- local env overrides live in the JSON files in this folder
