Parameter Golf asks for the best language model that fits inside a strict artifact budget.
This local sample corpus only exists to exercise the starter code and smoke test training.
The eventual training runs should replace this file with FineWeb shards or another challenge-compatible export.

Looped transformers reuse the same block many times so the effective depth grows without copying parameters.
Muon applies an orthogonalized update to matrix-shaped parameters while AdamW handles embeddings and norms.
Quantization-aware training can be enabled near the end of training so the final int8 checkpoint keeps more quality.

This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
This sample text intentionally repeats the same themes.
